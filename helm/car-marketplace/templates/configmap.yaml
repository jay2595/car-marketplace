apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "car-marketplace.fullname" . }}-config
  labels:
    {{- include "car-marketplace.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
